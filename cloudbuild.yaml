steps:
  # Step 1: Clone the repository
  - name: 'gcr.io/cloud-builders/git'
    args: ['clone', '--depth', '1', 'https://github.com/JAY-reddy1029/nvidia-cdc-framework.git', '/workspace/repo']
    id: 'clone-repo'

  # Step 2: Install Dataform CLI globally
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install', '-g', '@dataform/cli']
    id: 'install-dataform-cli'

  # Step 3: Compile Dataform workspace
  - name: 'gcr.io/cloud-builders/npm'
    dir: '/workspace/repo'
    args: ['run', 'dataform', 'compile', '--dir=dataform']
    id: 'dataform-compile'

  # Step 4: Deploy to production
  - name: 'gcr.io/cloud-builders/npm'
    dir: '/workspace/repo'
    args:
      - 'run'
      - 'dataform'
      - 'release'
      - 'deploy'
      - '--project-id=project-a2f79f6e-db67-4726-be8'
      - '--release-config-name=production'
    env:
      - 'DATAFORM_CORE_VERSION=3.0.52'
    id: 'dataform-deploy'

# Build timeout
timeout: '1800s'

# Build options
options:
  logging: CLOUD_LOGGING_ONLY

# Substitutions
substitutions:
  _PROJECT_ID: 'project-a2f79f6e-db67-4726-be8'
  _DATAFORM_VERSION: '3.0.52'
  _RELEASE_CONFIG: 'production'
